<section>
    <div>
        <div class="ban">
            <p>
                <span class="block">
                    ¿Te gustaría crear un proyecto con nosotros?
                </span>
                <span class="color">
                    Solicita un presupuesto para tu página web.
                </span>
            </p>
            <div class="shrink">
                <div class="button-container">
                    <button type="button">
                       Cotizar
                    </button>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
     section {
        margin-top: 1rem;
        margin-left: auto;
        margin-right: auto;
        padding: 3rem 2rem;
     }
    
    div {
        border-radius: .70rem;
        background-color: var(--color2); 
    }
    .ban {
        z-index: 20; 
        padding-left: 1rem;
        padding-right: 1rem; 
        padding-top: 3rem;
        padding-bottom: 3rem; 
        width: 100%; 
        text-align: center; 
    }
    .ban p {
        font-size: 1.5rem;
        line-height: 2.25rem; 
        font-weight: 800; 
        color: #f0f0f0; 
    }
    .ban span {
        display: block;
    }
    .ban .color {
        font-size: 1.3rem;
    }
    .shirink {
        flex-shrink: 0;
        margin-top: 0; 
    }
    .button-container {
        display: inline-flex; 
        margin-top: 3rem; 
        border-radius: 0.375rem; 
        box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06); 
    }
    .button-container button {
        padding-top: 1rem;
        padding-bottom: 1rem; 
        padding-left: 1.5rem;
        padding-right: 1.5rem; 
        border-radius: 0.5rem; 
        width: 100%; 
        font-size: 1rem;
        line-height: 1.5rem; 
        font-weight: 600; 
        text-align: center; 
        color: var(--color2); 
        background-color: whitesmoke; 
        transition-property: background-color, border-color, color, fill, stroke, opacity, box-shadow, transform;
        transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
        transition-duration: 300ms; 
        transition-duration: 200ms; 
        transition-timing-function: cubic-bezier(0.4, 0, 1, 1); 
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06); 
    }

    .button-container button:hover {
        background-color: #4338CA; 
    }
    @media (min-width: 640px) { 
        section {
        padding: 3rem 1rem;
    }
    .ban {
        padding-left: 1.5rem;
        padding-right: 1.5rem; 
    }
    .ban p { 
        font-size: 1.4rem;
    }
    .ban .color {
        font-size: 1.2rem;
    }
 }

@media (min-width: 1024px) { 
    .ban {
        padding-left: 2rem;
        padding-right: 2rem; 
        padding-top: 4rem;
        padding-bottom: 4rem; 
    }
    .ban p { 
        font-size: 2.2rem;
    }
    .ban .color {
        font-size: 1.56rem;
    }
    section {
        margin-top: 1rem;
        padding: 3rem 4rem;
    }
 }
</style>